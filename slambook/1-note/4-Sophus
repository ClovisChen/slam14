罗德里格斯公式  有一个旋转轴为 n，角度为 θ 的旋转，显然，它对应的旋转向量为 θn
  R = cos θI + (1 − cos θ) nnT + sin θn∧为由旋转向量到旋转矩阵、
  eigen
李群李代数：指数映射 李代数上的求导(两种求导方式)--求导和扰动
BCH--Baker-Campbell-Hausdorff 矩阵乘数之积会产生一些余项

strasdat维护的sophus库--李代数库（在eigen基础上开发的）（确保是非模板类-精度）
